<% layout("layouts/Boilerplate") %>

<div class="container">
  <div id="map" style="width: 100%; height: 400px"></div>

  <h1 class="mt-3">🏢 All Coworking Spaces</h1>

  <!-- <ul>
    <% for(let coworkingSpace of coworkingSpaces) { %>

    <div class="card mb-3 rounded">
      <div class="row">
        <div class="col-md-4">
          <img src="<%= coworkingSpace.images[0].url %>" alt="" class="img-fluid" />
        </div>

        <div class="col-md-8">
          <h5 class="card-title"><%= coworkingSpace.title %></h5>
          <p class="card-text"><%= coworkingSpace.description %></p>
          <p class="card-text">
            <small class="text-muted"> <%= coworkingSpace.location %> </small>
          </p>

          <a href="/coworkingSpaces/<%= coworkingSpace._id %>" class="btn btn-primary">
            <%= coworkingSpace.title %>
          </a>
        </div>
      </div>
    </div>

    <% } %>
  </ul> -->

  <div class="row">
    <div class="col-12 mt-3">
      <!-- Prev MAIN HERE -->

      <% coworkingSpaces.forEach((coworkingSpace, i) => { %> <% if (i%2 === 0 &&
      i !=0 ) { %>
    </div>
    <% } %> <% if (i%2 === 0) { %>
    <div class="row mb-md-2">
      <% } %>

      <div class="col-md-6 mt-md-0 mt-3">
        <div class="card shadow rounded-corner p-3">
          <div class="row">
            <div class="col-md-4">
              <img
                src="<%= coworkingSpace.images[0].url %>"
                alt=""
                class="img-fluid"
              />
            </div>

            <div class="col-md-8">
              <h5 class="card-title"><%= coworkingSpace.title %></h5>
              <p class="card-text"><%= coworkingSpace.description %></p>
              <p class="card-text">
                <small class="text-muted">
                  <%= coworkingSpace.location %>
                </small>
              </p>

              <a
                href="/coworkingSpaces/<%= coworkingSpace._id %>"
                class="btn css-button-rounded--blue"
              >
                <%= coworkingSpace.title %>
              </a>
            </div>
          </div>
        </div>
      </div>

      <% }) %>

      <!-- PREV END HERE
 -->
      <!-- </div> -->

      <!-- <div class="mt-4">
            <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#reorderModal">
                Change Content Order
            </button>
        </div> -->
    </div>
  </div>
</div>

<script>
  const mapToken = "<%-process.env.MAPBOX_TOKEN%>";
  let coworkingSpaces = {
    features: <%- JSON.stringify(coworkingSpaces) %>,
  };
</script>

<script src="/js/clusterMap.js"></script>
